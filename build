#!/usr/bin/env python3

import os

vendor = "157239n"
images = ["basic", "apache", "php", "mysql", "proxy", "nginx"]

for image in images:
	os.system("docker image build --tag " + vendor + "/" + image + " " + image + "/")
